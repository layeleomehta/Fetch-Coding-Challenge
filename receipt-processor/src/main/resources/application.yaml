
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fetch_challenge
    username: postgres
    password:

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080
